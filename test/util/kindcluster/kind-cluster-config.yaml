# three node (two workers) cluster config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
    - containerPort: 30007
      hostPort: 30007       # open test-kubegres-primary host port for kind
    - containerPort: 30008
      hostPort: 30008       # open test-kubegres-replica host port for kind
- role: worker
- role: worker
- role: worker
- role: worker
